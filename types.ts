export type CandleTime = '1m' | '5m' | '15m'

export enum DiffUnit {
  SECOND = 'second',
  MINUTE = 'minute',
  HOUR = 'hour',
  DAY = 'day',
  MONTH = 'month'
}

export enum ChartIndex {
  HIGH = 2,
  LOW = 3,
  CLOSE = 4,
  VOLUME = 5
}
